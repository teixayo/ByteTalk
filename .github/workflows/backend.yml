name: Backend CI & Deploy

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'

jobs:
  test:
    name: Test Backend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Run tests
        run: ./gradlew test
        working-directory: ./backend

  deploy:
    name: Deploy Backend
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        run: echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & push Docker image
        run: |
          docker build -t teixayo/bytetalk-backend:latest ./backend
          docker push teixayo/bytetalk-backend:latest

      - name: Trigger production webhook
        run: |
          curl -X POST ${{ secrets.PRODUCTION_BACKEND_WEBHOOK_URL }} \
               -H "Authorization: Bearer ${{ secrets.BACKEND_WEBHOOK_SECRET }}"
